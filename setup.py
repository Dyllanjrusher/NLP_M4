from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A google based review scraper that analyzes the reviews of products and performs sentiment analysis on them in order to determine the best reviewed products by sentiment instead of just th e highest rated product.',
    author='Dyllan USher',
    license='',
)
